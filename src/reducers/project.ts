import {
    ProjectActionTypes,
    LIST_PROJECTS,
    NEW_PROJECT
} from '../store/types'

import { Project } from "../types";

export function projectReducer(
    action: ProjectActionTypes
) {
    switch (action.type) {
        case LIST_PROJECTS:
    }
}